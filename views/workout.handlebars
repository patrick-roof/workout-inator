<section class="container">
  <h3></h3>
  <section class="workout">
    <img src="/images/{{workout.filename}}" alt="{{workout.description}}">
    <div class="workout-details">
      <h3>{{workout.muscle_group_focus}}</h3>
      <p>{{workout.exercise}}</p>
      <p>🦾🏋️‍♀️🥇🏆: {{workout.description}}</p>
    </div>
  </section>
</section>