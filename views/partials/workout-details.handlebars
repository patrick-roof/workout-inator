<section class="card"> <!--add bulma or add CSS-->
    <a href="/workout/{{id}}">
      <header>{{exercise}}</header>
      <h2>{{title}}</h2>
      <div class="card-body"> <!--add bulma or add CSS-->
      <img src="/images/{{filename}}" alt="{{description}}">
      <h3>Muscle Group:{{muscle_group_focus}}</h3>
      <p>🦾🏋️‍♀️🥇🏆:{{description}}</p>
      </div>
    </a>
</section>